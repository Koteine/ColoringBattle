meta:
  environment: node
  toolchain:
    name: npm
    version: 20

run:
  command: npm start
  containerPort: 8080
  persistenceMount: /data

persistence:
  enabled: true
  mountPath: /data
  path: /data
